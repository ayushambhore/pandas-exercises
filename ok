import pandas as pd

# Sample: Assuming df1 and df2 are your DataFrames and both have a common 'id' column
# Ensure they are sorted by 'id' to align comparisons correctly
df1_sorted = df1.sort_values('id').reset_index(drop=True)
df2_sorted = df2.sort_values('id').reset_index(drop=True)

# Create an empty dictionary to store differences
diff_info = {}

# Loop through each column (excluding 'id')
for column in df1.columns:
    if column == 'id':
        continue

    # Compare values for the column
    mask = df1_sorted[column] != df2_sorted[column]

    # If differences exist, store the count and ids
    if mask.any():
        differing_ids = df1_sorted.loc[mask, 'id'].tolist()
        diff_info[column] = {
            'count': len(differing_ids),
            'ids': differing_ids
        }

# Convert to DataFrame for display (optional)
diff_df = pd.DataFrame([
    {'column': col, 'count': info['count'], 'ids': info['ids']}
    for col, info in diff_info.items()
])
