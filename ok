import pandas as pd

# Assuming `df_merge` is your DataFrame and `columns` is your list of factor columns

# Step 1: Melt the DataFrame to long format
melted = df_merge.melt(id_vars=['Date', 'GRID', 'UnivID'], value_vars=columns,
                       var_name='FactorName', value_name='FactorValue')

# Step 2: Group by Date and FactorName
group = melted.groupby(['Date', 'FactorName'])

# Step 3: Aggregate counts
summary = group.agg(
    UnivCount=('GRID', 'count'),              # Total number of records (universities)
    FactorCount=('FactorValue', 'count')      # Non-null values for the factor
).reset_index()

# Step 4: Add Coverage column
summary['Coverage'] = round((summary['FactorCount'] / summary['UnivCount']) * 100, 4)

# Optional: Get UnivID (assuming it's the same for all rows per group — if not, this needs adjusting)
first_univ = melted.groupby(['Date', 'FactorName'])['UnivID'].first().reset_index()
final_df = pd.merge(summary, first_univ, on=['Date', 'FactorName'])

# Rearranging columns
final_df = final_df[['Date', 'UnivID', 'UnivCount', 'FactorName', 'FactorCount', 'Coverage']]
